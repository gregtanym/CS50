// creating a function that splits the contents of a linked list into two other linked lists, even and odd
// since the lists already exist, maybe there is no need to malloc any new space?

int split(node* head, node* even_list, node* odd_list)
{
    int count = 0;
    node* current_original = head;
    node* current_even = even_list;
    node* current_odd = odd_list;
    
    do 
    {
        
        
        
        
        if(count % 2 = 0)
        {
            even_list->number = tmp_pointer->number;
            even_list->next = malloc(sizeof(node));
        }
        
        else
        {
            odd_list->number = tmp_pointer->number;
            odd_list
        }
        
        
        
        tmp_pointer = tmp_pointer->next;
        
        count++;
    }
    while(*node point != NULL)
}